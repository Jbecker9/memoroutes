import React from "react";

function CityDestination({ destination }){

    return(
        <div>
            <h5>{ destination.location_name }</h5>
        </div>
    )
}

export default CityDestination